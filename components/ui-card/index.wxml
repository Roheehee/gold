<view class="ui-card" style="padding: {{padding}}">
  <view wx:if="{{title || desc}}" class="ui-card__head">
    <text wx:if="{{title}}" class="ui-card__title">{{title}}</text>
    <text wx:if="{{desc}}" class="ui-card__desc">{{desc}}</text>
  </view>
  <slot />
</view>
